## 1. Retrieval services — test-first, real embedding values, no live services required

- [x] 1.1 Capture a small set of real embeddings **once**, via the running `embedder` (a one-time script, not part of the test run itself) — reuse the exact query/content pairs already verified empirically earlier in this session ("copy files to a remote host" as a query; real `ansible.builtin.copy::param::src`/`dest`, `ansible.builtin.uri::param::dest`, `ansible.builtin.unarchive::param::src` chunk content) — store as a test fixture (text → 384-float vector) — wrote `test/fixtures/files/embeddings.json` (6 entries, real content pulled from the production DB); sanity-checked cosine similarity against the query: `copy::param::src`/`dest` highest (0.62/0.57), `uri::param::dest`/`unarchive::param::src` moderate (0.53/0.50), an added unrelated distractor `user::param::shell` nearly orthogonal (0.03) — confirms the fixture encodes real, meaningful semantic separation
- [x] 1.2 Write `Retrieval::KeywordSearch` tests — seed a handful of `Chunk` rows directly (`Chunk.create!`, real-sounding text content, no embeddings needed — pure Postgres full-text search) — confirm a known query surfaces the expected chunks. No live services needed. (Added a `test/support/retrieval_test_helpers.rb`, auto-loaded via `test_helper.rb`, for the chunk-seeding boilerplate shared across this whole change's tests.)
- [x] 1.3 Implement `Retrieval::KeywordSearch` (thin wrapper over the existing `Chunk.search`) — `scope.search(query)`, confirmed Rails' class-method-delegates-to-relation behavior works correctly for `scope:` narrowing
- [x] 1.4 Write `Retrieval::VectorSearch` tests — seed `Chunk` rows using the captured real embeddings (1.1), stub `EmbeddingClient.embed` (`webmock`) to return the matching captured vector for the test's query text — confirm a semantically-similar-but-keyword-different chunk still ranks highly. No live `embedder` needed.
- [x] 1.5 Implement `Retrieval::VectorSearch` (embeds via the existing `EmbeddingClient`, calls `Chunk.nearest_neighbors(:embedding, embedding, distance: "cosine")` — the verified class-level scope) — confirmed `scope.nearest_neighbors(...)` chains correctly onto an arbitrary relation, not just bare `Chunk`
- [x] 1.6 Write `Retrieval::HybridSearch` tests — confirm the merged ranking reflects both keyword and vector results (not identical to either alone), confirm `scope:` correctly narrows results to one module's chunks, and confirm each returned result carries its RRF score (not bare `Chunk` records) — designed the fixture so keyword search's `WHERE` clause fully excludes `uri::dest` while vector search still finds it meaningfully close, proving RRF adds real signal, not just duplicating one ranking (verified via a dedicated sanity-check test on the fixture itself)
- [x] 1.7 Implement `Retrieval::HybridSearch` (RRF, k=60, `scope:`/`limit:` params; returns `Retrieval::ScoredChunk` — `Struct.new(:chunk, :rrf_score)` — so the score survives past the merge step for `RetrievalLog` to use later) — real run against the fixture surfaced that `copy::dest` narrowly out-scored `copy::src` for top rank (real `ts_rank` weighting, not predictable by eyeballing text); fixed a too-fragile test assertion that had hard-coded which of two similarly-relevant chunks should win, asserting the correct *module* wins instead

## 2. RerankerClient — test-first

- [x] 2.1 Write `RerankerClient` tests first, using `webmock` (mirroring `EmbeddingClient`'s existing test suite): order-preservation, empty-input, retry-triggering transient failure
- [x] 2.2 Implement `RerankerClient` (Faraday + `faraday-retry`, `methods: [:post]`, `POST /rerank`) — mirrors `EmbeddingClient` almost exactly, as planned

## 3. Tools — test-first (subprocess/HTTP-adjacent orchestration logic)

- [x] 3.1 Write `SearchAnsibleDocs` tests first, exercising real retrieval logic against the captured-embedding fixtures (1.1) with `EmbeddingClient`/`RerankerClient` stubbed via `webmock` — no live `embedder` needed here either: confirms hybrid+rerank runs, `module_filter:` narrows results, and the tool instance accumulates `{stable_id, rrf_score, rerank_score}` for every returned chunk across calls within one instance's lifetime (stubbed reranker deliberately reverses hybrid order, so a passing test proves reranking actually ran, not just hybrid order passed through untouched)
- [x] 3.2 Implement `SearchAnsibleDocs` (`RubyLLM::Tool`, `query:`/`module_filter:`, always hybrid+rerank, no strategy option; pairs each `HybridSearch` result's `rrf_score` back up with its `RerankerClient` score) — all 3 tests from 3.1 pass, rubocop clean
- [x] 3.3 Write `GetModuleDetails` tests first: found case returns `raw_doc`, not-found case returns a clear message rather than raising — confirmed both fail with `NameError` (class doesn't exist yet)
- [x] 3.4 Implement `GetModuleDetails` — `AnsibleModule.find_by(fqcn:)&.raw_doc` serialized to JSON, or a "not found" string; both tests pass, rubocop clean

## 4. RetrievalLog

- [x] 4.1 Migration + `RetrievalLog` model (`belongs_to :message`, `retrieval_strategy`, `retrieved_chunks` as `jsonb` — array of `{stable_id, rrf_score, rerank_score}` objects, `stable_id` not numeric `Chunk.id`, both the hybrid-merge and reranker scores kept (see design.md's "Corrected during design" note) — `ansible_core_version`, `cost`, `response_time`, `input_tokens`, `output_tokens`, `top_module`; unique index on `message_id`, mirroring `Feedback`'s pattern) — added `Message#retrieval_log` reverse `has_one`, migrated dev DB and confirmed generated schema.rb matches
- [x] 4.2 Model tests: associations, presence validation on `retrieval_strategy`, `retrieved_chunks` default/round-trip, unique-index-per-message — 5 tests pass, rubocop clean

## 5. Wire tools + RetrievalLog into ChatResponseJob — test-first

- [x] 5.1 Write `ChatResponseJob` tests first (LLM stubbed via the established `webmock`/SSE pattern, real retrieval logic underneath via the same captured-embedding-fixtures approach as section 1/3 — no live `embedder` needed anywhere in this suite): a reply that triggers `SearchAnsibleDocs` gets a `RetrievalLog` with correct `retrieved_chunks` (`stable_id`+`rrf_score`+`rerank_score` triples)/`ansible_core_version`/`input_tokens`/`output_tokens`/`top_module`; a reply that doesn't call any tool gets no `RetrievalLog` row at all (not an empty one) — had to hand-construct the two-chunk OpenAI streaming tool-call SSE shape (start delta carries `id`+`function.name`, continuation delta carries only `function.arguments`, matching `parse_tool_calls`' id-presence-based accumulation confirmed by reading the installed `ruby_llm` gem source) plus a second queued SSE response for the follow-up completion after tool execution; confirmed both new tests fail/pass correctly pre-implementation (tool-call plumbing itself already worked via `with_tools`, only `RetrievalLog` creation was missing)
- [x] 5.2 Implement: instantiate `SearchAnsibleDocs`/`GetModuleDetails`, register via `chat.with_tools(search_tool, GetModuleDetails)`, time the call for `response_time`, read `chat.cost.total` for `cost` and `chat.messages.last.input_tokens`/`.output_tokens` for token counts, build `RetrievalLog` from the tool instance's accumulated results — added `SearchAnsibleDocs#last_call_top_module` (computed from the already-sorted `ranked` array's first entry, no extra query) to support `top_module` without polluting the persisted `retrieved_chunks` shape with extra keys; `ansible_core_version` resolved via one `Chunk.find_by(stable_id:)` lookup on any accumulated chunk (all chunks share one version per ingestion run); all 5 `ChatResponseJob` tests pass, full suite (59 tests) green, rubocop clean

## 6. Citations in the UI

- [x] 6.1 Add a citations partial reading from an assistant message's `RetrievalLog`, grouped by module (e.g. "Sources: `ansible.builtin.copy` (mode, owner)") — resolve `retrieved_chunks`' `stable_id`s via `Chunk.where(stable_id: ...)` (never a raising per-ID lookup); `score` isn't shown in the UI, only stored for later eval/debugging use — added `MessagesHelper#citation_groups` (plain `where`, `group_by` on `ansible_module.fqcn`) and `messages/_citations.html.erb`
- [x] 6.2 Render it in `_assistant.html.erb`, only when a `RetrievalLog` exists for that message — guarded with `if assistant.retrieval_log`
- [x] 6.3 Style with Tailwind, matching `enable-tailwindcss`'s existing conventions — reused the existing `text-gray-500`/`text-sm` footer style, fqcn in a `<code>` chip matching this app's other inline-code styling
- [x] 6.4 Test: citation rendering for a `RetrievalLog` whose `retrieved_chunks` includes a `stable_id` that no longer resolves to any `Chunk` doesn't raise, and simply omits that source — `test/helpers/messages_helper_test.rb` (3 tests), plus confirmed `_assistant.html.erb` still renders correctly with no `retrieval_log` present via the existing `ChatsControllerTest`; full suite (62 tests) green, rubocop clean

## 7. Verification

- [x] 7.1 Run the full test suite, rubocop, and brakeman — 62 tests/197 assertions green, `bin/rubocop -a` cleaned up 8 pre-existing `Layout/SpaceInsideArrayLiteralBrackets` offenses across earlier sections' files (0 remaining), brakeman: 0 warnings
- [x] 7.2 Rebuild `web`/`worker` images, bring up the full stack — also had to rebuild `prepare_db` separately (it builds from the `web` target under its own image tag, per this sandbox's podman quirks) and re-run `bin/rails db:prepare` directly (`--requires` dependency-graph resolution failed after container recreation, a known sandbox-only podman bug per CLAUDE.md — worked around with a direct `podman run` using the same image/env/network); confirmed `retrieval_logs` migrated into the real `rails_app_production` DB
- [x] 7.3 Ask a real question requiring a specific doc lookup ("What does the mode parameter of the copy module default to?") through the real running app; confirmed the answer is correct (umask-default / preserved-if-exists / `mode=preserve` — matches real Ansible docs), a citation renders ("Sources: `ansible.builtin.copy` (1, 2, 3, overview, directory_mode, mode, remote_src, src)"), and the `RetrievalLog` row has 8 `retrieved_chunks`, `ansible_core_version: "2.21.2"`, `top_module: "ansible.builtin.copy"`, real non-null `cost`/`response_time`/`input_tokens`/`output_tokens` — verified directly via `psql` against the real running container
- [x] 7.4 Asked "Hi, how are you?" (no retrieval needed): normal reply, no citations block rendered, confirmed 0 `RetrievalLog` rows for that chat via `psql`
- [x] 7.5 Real multi-turn check: same chat as 7.3, follow-up "And what about the owner parameter I asked about the copy module before - does it accept a username or must it be a UID?" (no module name restated) correctly resolved "the copy module" from prior turns, retrieved the right `owner` parameter chunk, gave an accurate answer, and rendered its own citation
- [x] 7.6 Confirmed via `podman logs` on the real `worker` container across all three live checks above: `ChatResponseJob` executes cleanly with `SearchAnsibleDocs`/`GetModuleDetails` registered, no errors, correct `RetrievalLog` creation and Turbo Stream broadcast back to the browser
