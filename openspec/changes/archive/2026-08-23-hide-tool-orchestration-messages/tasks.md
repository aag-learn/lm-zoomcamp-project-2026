## 1. Tests first

- [x] 1.1 Write a test asserting `render` on a `role: "tool"` message and on an assistant message with `tool_calls` attached both produce blank/empty output — seed the minimal records needed (a tool-call message with an associated `ToolCall`, a `role: "tool"` result message), render each via the real partial dispatch (`ActionView::TestCase` or equivalent), assert the rendered output is empty (or contains none of the raw tool name / raw content that would appear today). New file `test/views/messages_rendering_test.rb`.
- [x] 1.2 Write an integration test (extending `ChatsControllerTest`'s existing pattern) that seeds a full tool-using turn — user message, assistant-with-tool-calls message + its `ToolCall`, a `role: "tool"` result message with distinctive raw content, and a final assistant message with distinctive real-answer content — `GET` the chat page, and assert the response body includes the user message and the final assistant content, but does NOT include the tool call's raw arguments text or the tool result's raw content text
- [x] 1.3 Confirmed both new tests fail against the current (unfixed) partials, for the expected reason (today's partials render the raw content, not blank).

## 2. Implement

- [x] 2.1 Replace the contents of `app/views/messages/_tool_calls.html.erb` and `app/views/messages/_tool.html.erb` with an explanatory ERB comment only (no markup) — per design.md, this is the single change point that fixes both live-streaming broadcast and full-page-reload rendering, since both route through `Message#to_partial_path`
- [x] 2.2 Delete the now-unreachable `app/views/messages/tool_calls/_default.html.erb` and `app/views/messages/tool_results/_default.html.erb`
- [x] 2.3 Delete the now-unreachable `MessagesHelper#tool_call_partial`, `#tool_result_partial`, and `#partial_for` (confirmed during design: no other callers)
- [x] 2.4 (Discovered during 2.2, see design.md.) Blank `app/views/messages/_system.html.erb` the same way — `guarantee-retrieval-grounding`'s `chat.with_instructions` now persists a `role: "system"` message on every turn, which was rendering as a full visible "System" bubble showing the raw instructions text. Delete the now-orphaned `app/views/messages/_error.html.erb` (only caller was the just-deleted `tool_results/_default.html.erb`).
- [x] 2.5 Add a test asserting a `role: "system"` message renders blank (extend `test/views/messages_rendering_test.rb`)
- [x] 2.6 Run the tests from section 1 (plus 2.5), confirm they now pass. (5/5 passing.)

## 3. Verification

- [x] 3.1 Ran the full test suite (101/101 passing), rubocop (clean on all files this change touched — a pre-existing, unrelated offense in `config/application.rb` from the earlier `local-dev-environment` change was left alone, out of scope), and brakeman (zero warnings; nonzero exit code was `Not_Latest_Version_Exit_Code`, a stale-gem-version notice, confirmed by reading `brakeman.rb`'s exit-code constants).
- [x] 3.2 Used the local dev workflow (postgres + local `embedder` + `bin/dev`) rather than a full container rebuild — same Rails codebase, same rendering mechanism, faster iteration; this is exactly what `local-dev-environment` built this path for. Replayed this session's exact repro ("how can I install a brew package using ansible") through the real running app via real HTTP requests (fetched CSRF token, posted the message, waited for the worker to finish). The real run took 5 rounds of tool calls (the model tried `community.general.homebrew`/`homebrew` before landing on `ansible.builtin.apt`+`ansible.builtin.tempfile`) — 14 messages persisted total (1 system, 2 user, 5 tool_calls, 5 tool, 1 final assistant), confirmed via `to_partial_path` on each. Reloaded the page: exactly 3 message bubbles rendered (2 user + 1 final assistant) — grepped for raw tool-call/tool-result/system content, none found. Live-streaming-specific confirmation (vs. reload) wasn't separately observed via a websocket client, but design.md's core finding (both paths dispatch through the same `to_partial_path`) is independently verified by the unit tests in section 1, so this isn't a meaningfully separate risk.
- [x] 3.3 (Adapted — see note.) `guarantee-retrieval-grounding` (already merged) forces `SearchAnsibleDocs` on the first round of every turn, so "a question that doesn't trigger any tool" no longer exists in production — the original premise of this task is now unreachable. Adapted to the closest still-relevant regression check: a simple question that triggers exactly one round of tool calls (not the multi-retry pattern above). Confirmed: 5 messages persisted (1 system, 1 user, 1 tool_calls, 1 tool, 1 final assistant), reload shows exactly 2 bubbles (user + final answer), no leakage.
- [x] 3.4 (Discovered during 2.2.) Confirmed via the same two live runs above — no "System" bubble appeared at any point, initial load or after reload, despite every turn now having a real `role: "system"` message.

## 4. Retrieval-details popup

- [x] 4.1 Write a test asserting an assistant message with a `RetrievalLog` renders a retrieval-details affordance (not an inline "Sources:" line), and an assistant message with no `RetrievalLog` renders neither
- [x] 4.2 Write a test that the dialog's rendered content includes the log's retrieved chunks (stable_id + rerank_score), top_module, ansible_core_version, cost, and response_time — including for a log whose scores are all low/negative (no suppression). (No system-test infra exists in this repo yet — `capybara`/`selenium-webdriver` are in the Gemfile but never scaffolded, no `test/system` dir. Used a content-presence assertion on the dialog's rendered HTML instead, consistent with this app's existing test style; the actual click-to-open interaction is covered live in 4.7.)
- [x] 4.3 Confirmed both fail against the current `_citations.html.erb` (which auto-renders instead).
- [x] 4.4 Replace `_citations.html.erb`'s auto-rendered line with the popup trigger; add the `<dialog>` markup and its Stimulus controller (`app/javascript/controllers/retrieval_details_controller.js`, auto-registered via the existing importmap `eagerLoadControllersFrom` convention — no new pin needed).
- [x] 4.5 `_assistant.html.erb`'s `if assistant.retrieval_log` guard is still required (the new partial dereferences `log.top_module` etc., which would raise on nil) — confirmed no change needed.
- [x] 4.6 Run the tests from 4.1/4.2, confirm they pass. (6/6 passing.)
- [x] 4.7 Live-checked against the real running app: chat 14's reply (real `RetrievalLog`) shows a "View retrieval details" button; the served page's `<dl>` shows real data (`ansible.builtin.copy`, `2.21.3`, `$0.0012`, `9.9s`) and all 8 retrieved chunks with their real (mostly negative) rerank scores shown unfiltered, exactly matching the "no suppression" design decision. The Stimulus controller asset (`/assets/controllers/retrieval_details_controller-*.js`) is served correctly (200, correct content) via the existing importmap auto-discovery, no manual pin needed. Separately confirmed a pre-`guarantee-retrieval-grounding` chat (message with no `RetrievalLog`) shows no affordance and no leftover "Sources:" text.
