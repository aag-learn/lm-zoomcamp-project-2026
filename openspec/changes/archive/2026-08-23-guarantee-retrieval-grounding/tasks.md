## 1. Tests first

- [x] 1.1 Write a `ChatResponseJobTest` asserting the outgoing OpenAI request body includes `tool_choice: {"type":"function","function":{"name":"search_ansible_docs"}}` on the first completion request of a turn — stub via WebMock and inspect the captured request body, following the existing `stub_openai_chat`/`openai_tool_call_sse_body` helpers in that file
- [x] 1.2 Write a `ChatResponseJobTest` asserting the outgoing request includes a `role: "system"` message with the `with_instructions` content, and that a second `ChatResponseJob#perform` call on the same `Chat` does not duplicate it (still exactly one system message). (Discovered: RubyLLM's default `openai_use_system_role: false` sends this on the wire as `role: "developer"`, not `"system"` — the persisted `Message` row is still `role: :system`; test asserts on the wire role since that's what proves the instructions actually reached the model.)
- [x] 1.3 Write a `ChatResponseJobTest` asserting a `RetrievalLog` row is created even when `SearchAnsibleDocs` returns no relevant chunks — stub a tool-call round whose tool result is empty/irrelevant, and assert `RetrievalLog.count` increments with `retrieved_chunks` reflecting whatever was actually returned (not skipped). Replaced the old "a reply that doesn't call any tool gets no RetrievalLog row" test — that scenario (zero tool calls) is no longer reachable in practice once the first round always forces one.
- [x] 1.4 Write an `Eval::LlmEvaluatorTest` (new file, none existed) asserting `ask_with_tools` sends the same forced `tool_choice` on its request. (Discovered: `Eval::LlmEvaluator#ask_with_tools` calls `.ask` with no block, so RubyLLM sends `stream: false` and expects a single non-streaming JSON response, not the SSE format `ChatResponseJobTest`'s helpers produce — needed a separate non-streaming stub body.)
- [x] 1.5 Confirm all four new/extended tests fail against the current code, for the expected reason (no `tool_choice` sent, no system/developer message, `RetrievalLog` skipped). Verified directly: temporarily reverted both source files, ran the 5 affected tests, all failed for the expected reason (`tool_choice` nil, message count off by one, `RetrievalLog.count` unchanged by 0 instead of 1), then restored the real implementation and confirmed all 8 pass again.

## 2. Implement

- [x] 2.1 In `app/jobs/chat_response_job.rb#perform`, add `chat.with_instructions(...)` before `chat.ask(content)`, with the corpus-pinned-to-`ansible_core_version` reasoning from design.md
- [x] 2.2 In the same method, add `choice: :search_ansible_docs` to the existing `chat.with_tools(search_tool, GetModuleDetails)` call
- [x] 2.3 Remove the `if search_tool.retrieved_chunks.any?` guard around `build_retrieval_log(...)` so it always fires. Also guarded `ansible_core_version` lookup against `retrieved_chunks` being empty (`first_chunk = ...first; first_chunk && Chunk.find_by(...)`) — the old code's `.first[:stable_id]` would raise `NoMethodError` on `nil` once this path is reachable with zero chunks.
- [x] 2.4 In `app/services/eval/llm_evaluator.rb#ask_with_tools`, add `choice: :search_ansible_docs` to its `with_tools` call
- [x] 2.5 Remove the "low confidence rate" panel from `grafana/dashboards/chat-monitoring.json`; confirmed the remaining JSON is still valid (5 panels remain: Query volume, Latency, Cost, Feedback ratio, Top modules queried). No orphaned `templating` entries referenced it.
- [x] 2.6 Run the tests from section 1, confirm they now pass. (8/8 passing.)

## 3. Verification

- [x] 3.1 Run the full test suite, rubocop, and brakeman. 97/97 tests pass; rubocop clean on all changed files; brakeman reports zero warnings (its nonzero exit code was `Not_Latest_Version_Exit_Code` — a stale-gem-version notice unrelated to any finding, confirmed by reading `brakeman.rb`'s exit-code constants).
- [x] 3.2 Against a running local stack (postgres + local `embedder`), replayed `PLAN.md`'s canonical example question ("what does the mode parameter of the copy module default to?") through the real `ChatResponseJob` with a real OpenAI call. Confirmed: `tool_calls made: ["search_ansible_docs"]` (previously zero `SearchAnsibleDocs` calls for this exact question), `RetrievalLog` created with `top_module: "ansible.builtin.copy"`, 8 retrieved chunks, and a correct grounded answer (umask/preserved-if-exists behavior).
- [x] 3.3 Asked a plain greeting ("Hi, how are you?") through the real `ChatResponseJob`. Reply stayed a normal greeting ("I'm doing well, thanks! How can I help you today...") — not derailed by the forced search. A `RetrievalLog` row was created (`top_module: "ansible.builtin.ping"`, plausible vocabulary overlap with that module's connectivity-check doc text pushing its rerank score higher than expected, ~4.86 rather than deeply negative) — confirms the "no content-gating, trust the LLM's judgment" design decision holds in practice: real (if marginal) retrieved content was fed to the model, and it correctly ignored it rather than being derailed. Citation UI still auto-renders for now, as expected/accepted (see design.md Risks) until `hide-tool-orchestration-messages` lands.
- [x] 3.4 Ran `Eval::LlmEvaluator.call(limit: 6)` (a real, stratified sample across modules) and inspected results. Clear evidence of real grounding: RAG answers consistently include specific, doc-accurate facts ("Type: str", exact allowed values, exact defaults) that the no-RAG condition either hedges on or gets wrong. Headline example produced live: for the `iptables` module's `syn` parameter, the RAG answer correctly says "string, allowed values: ignore/match/negate" while the no-RAG answer confidently says "boolean" — a real, concrete RAG-vs-no-RAG divergence on this sample (`rag_accuracy: 0.833`, `no_rag_accuracy: 0.833` overall on this small sample, but with meaningfully different failure patterns per-question, e.g. the `syn` case).
- [x] 3.5 Started the local `grafana` container; confirmed via `GET /api/dashboards/uid/chat-monitoring` that it picked up the panel removal automatically via its existing file-polling provisioning (5 panels remain, "Low-confidence-retrieval rate/day" gone) — no container restart needed. Generated 5 real chat turns via `ChatResponseJob` (2 clearly on-topic, 3 clearly off-topic: "what's the weather like today?", "can you tell me a joke?", plus the earlier greeting). Queried `retrieval_logs` directly: `total_logs: 5`, `empty_retrieval_logs: 0` — all 5 real turns are now logged (previously, under the old gated logic, most of the 3 off-topic turns would likely have produced zero tool calls and thus no log row at all, undercounting exactly as design.md describes).
- [x] 3.6 `openspec validate --type change --strict guarantee-retrieval-grounding` passes.
