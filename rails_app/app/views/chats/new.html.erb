<% content_for :title, "New chat" %>

<div class="mx-auto w-full max-w-xl">
  <h1 class="mb-4 text-2xl font-bold">New chat</h1>

  <%= render "form", chat: @chat %>

  <div class="mt-4">
    <%= link_to "Back to chats", chats_path, class: "text-blue-600 hover:underline" %>
  </div>
</div>
